/**
 * Admin UI kit — единый словарь backoffice.
 *
 * Примитивы переехали в общий `components/ui-kit.tsx` (используются и админкой,
 * и дашбордом артиста — оба рендерятся на платформенном шелле, без темы артиста).
 * Этот модуль остаётся точкой импорта для админских страниц: re-export всего кита.
 */
export * from '../ui-kit';
