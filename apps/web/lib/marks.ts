/**
 * Фирменные знаки продукта — контуры, а не компоненты.
 *
 * Контур живёт отдельно от разметки, потому что рисуют его РАЗНЫЕ движки: React рисует его
 * в `<svg>`, стенд материала — в canvas через `Path2D`, а завтра о нём спросит и мобилка.
 * Пока путь лежал внутри компонентов, копий набралось три, и разойтись по форме им ничего
 * не мешало — а знак у продукта один.
 *
 * Система координат — viewBox 24×24, обводка 2, скруглённые концы.
 */

/** Волна «Потока»: непрерывное радио из похожих треков. */
export const FLOW_MARK = 'M2 12 C4.5 6, 7.5 6, 10 12 C12.5 18, 15.5 18, 18 12 C20.5 6, 22 6, 22 12';

/** Сторона системы координат знака: по ней он масштабируется под нужный кегль. */
export const MARK_VIEWBOX = 24;
/** Толщина обводки в той же системе координат. */
export const MARK_STROKE = 2;
