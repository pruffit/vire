# Роадмап: интерактивность и UX

> ✅ **ЗАВЕРШЁН.** Все этапы (1–10) выполнены. Документ оставлен как исторический
> журнал решений по интерактиву/анимациям/UX — «как и почему сделано». Действующие
> принципы движения вынесены в [`ui-principles.md`](ui-principles.md); описание
> готовых фич — в [`features/`](features/README.md).
>
> _Изначальный процесс: этап → тест → коммит → отметка здесь. На момент работы прода
> ещё не было (коммиты шли прямо в `main`); ветки/CI/CD появились позже (см.
> [`roadmap-1.0.md`](roadmap-1.0.md), блок 7)._

Контекст: после внедрения motion-системы (`packages/ui/src/motion/`) собрали список
доводок и новых фич от Danya.

> 🎯 **Постоянный принцип** (по фидбэку Danya): закладывать **больше живости/интерактива**
> и **оптимистичные апдейты** по умолчанию в каждой фиче — менять UI сразу, откатывать
> при ошибке; не ждать сервер и не делать `router.refresh()` ради простого изменения.
> См. память `feedback-interactivity-optimistic`.

> ⚠️ У Danya включён системный Reduced Motion — анимации он видит только при
> выключенной настройке ОС (см. память `project-vire-reduced-motion`).

---

## Этап 1 — Мелкие UX-фиксы
- [x] **1.1 Кнопка «Отписаться» (/profile).** Текст-кнопку заменил на компактную
  иконку-«×» (w-7 h-7, opacity 40%→100% на ховере карточки, hover-красный, tooltip).
  Не крадёт место, видна и на тач. Файл: `app/profile/unfollow-button.tsx`.

## Этап 2 — Пресеты темы в редакторе профиля
- [x] **2.1 Пресеты цветов** — 8 готовых палитр (тёмные + светлые) свотчами над
  ручными color-полями; клик применяет фон/текст/акцент разом в live-preview.
  Ручной ввод сохранён. Файл: `app/dashboard/profile/edit-profile-form.tsx`.

## Этап 3 — Раскрывающийся поиск в навбаре
- [x] **3.1** Иконка поиска: плавный hover-scale (1.15, мягкая пружина) + tap.
- [x] **3.2** Клик → поле раскрывается влево (motion width 0→220), под ним быстрые
  результаты (топ-6, тот же `/api/v1/search`), клавиатура ↑↓/Enter, «Показать все»
  → `/search`, клик вне/Esc сворачивает. Новый `components/nav-search.tsx`,
  подключён в `components/nav.tsx` (убрана статичная `SearchIconLink`).

## Этап 4 — Переходы между страницами
- [x] **4.1** `app/template.tsx` — opacity-crossfade контента на каждый переход.
  Ключ: обёртка `h-full` (ЯВНАЯ высота), а не `min-h-full` — иначе процентная
  высота дочерних страниц схлопывается (это и ломало лейаут). Только opacity
  (без transform/filter) ради `fixed`-грейна. Проверено на живом сервере: главная,
  /artists, /search, страница артиста — 200, лейаут цел, краш-петли нет.
  _Ограничение:_ богаче (slide/scale/blur) нельзя — сломает `fixed` грейн профиля.

## Этап 5 — Кастомный плеер для видео-эмбедов
- [x] **5.1** Видео-фасад: своя оболочка под сайт (постер + кнопка play, ленивый
  iframe = перф-выигрыш). `lib/embed.ts` (`parseEmbed`/`activeEmbedUrl`), +5 тестов.
- [x] **5.2** Полностью кастомный плеер (YouTube): свои контролы через IFrame
  Player API — play/pause, перемотка (seek-бар), громкость + mute, фуллскрин,
  автоскрытие контролов, центральная play/replay. Нативные контролы YouTube
  скрыты (`controls=0`). `lib/youtube-api.ts` (загрузчик+типы),
  `components/video-player.tsx` (старый `video-embed.tsx` удалён).
- [x] **5.3 VK — кастомные контролы.** Сервисный ключ VK получен (`VK_SERVICE_TOKEN`
  в `apps/web/.env.local`). Сделано: постер фасада через `video.get`
  (`lib/vk-api.ts`, server-only, revalidate 1ч), управление плеером через
  VK Video Player API (`lib/vk-player-api.ts`: js_api=1 + videoplayer.js,
  поллинг getState как источник правды), общий `ControlsBar` с YouTube.
  Если скрипт VK не загрузился — деградация до нативных контролов.
  Парсер понимает новый домен vkvideo.ru. Проверено на живом видео.

## Этап 6 — Дашборд: редактирование релиза и треков
- [x] **6.1** Поля релиза уже редактируются (`EditReleaseForm`) — было готово.
- [x] **6.2** Менеджер треков: переименование (inline, optimistic), удаление
  (каскад зависимых строк в транзакции на уровне репозитория — FK без cascade;
  optimistic + подтверждение), порядок (▲▼, optimistic swap + персист 2 PATCH).
  Бэкенд: `ITrackRepository.findById/update/delete` + `DrizzleTrackRepository`,
  `TrackService.updateTrack/deleteTrack` (проверка владения), роут
  `app/api/v1/dashboard/tracks/[id]` (PATCH/DELETE). Фронт:
  `app/dashboard/releases/[id]/track-manager.tsx`. Тесты сервиса (+4, core 29).

## Этап 7 — Переосмысление ленты и главной
Решение Danya: главная = хаб, вбирает открытие (артисты, свежие релизы, грядущие)
и активность подписок; отдельные `/artists` и `/feed` как самоцель не нужны.
- [x] **7.1** Новая главная-хаб (`app/page.tsx`): hero+поиск, «Новое у тех, на кого
  подписан» (для вошедших), «Скоро выйдет», «Свежие релизы», «Артисты» (+«Все артисты»).
  Новые запросы `getLatestReleases`/`getUpcomingReleases` (`queries/discovery.ts`).
- [x] **7.2** Лента расширена в активность на главной; `/feed` убран из навбара
  (как и «Артисты»). Маршруты `/feed` и `/artists` оставлены для SEO/прямых ссылок.
- [x] **7.3** Главная: секция «Сейчас слушают» (live из Redis-присутствия:
  `listListening` SCAN + `lib/listening-now.ts` + `/api/v1/listening-now`,
  клиентский опрос 30с, исчезает когда никого; клик играет трек) и секция
  «По настроению» (чипы тегов → волна с mood-seed: `getWaveNextTrack(..., seedMood)`,
  `/api/v1/wave?mood=X`). Персональные рекомендации — отложены до каталога.

## Этап 8 — Оптимистичные апдейты (сквозной)
- [x] **8.1** Отписка на /profile — карточка исчезает мгновенно (exit-анимация),
  запрос в фоне, откат при ошибке. Новый клиентский `app/profile/followed-artists.tsx`,
  старый `unfollow-button.tsx` удалён, `router.refresh()` убран.
- [x] **8.2** Аудит мутаций дашборда: публикация релиза оптимистична («Опубликован ✓»
  сразу, откат + тост при ошибке; `publish-button.tsx`), менеджер треков и посты —
  откаты озвучены тостами, сетевые ошибки fetch перехвачены. Плейлисты тоже
  оптимистичны (галочка сразу, откат + тост).

## Этап 9 — Фокус, выделение, доступность, интерактив (полиш)
Аудит сайта на фокусы/доступность/отклик.
- [x] **9.1** Баг `:focus-visible` — убран принудительный `border-radius: 2px`
  (круглые аватары/кнопки больше не «квадратятся» при фокусе с клавиатуры);
  кольцо фокуса контрастнее (color-mix от foreground). `app/globals.css`.
- [x] **9.2** Тематическое выделение текста `::selection` — подхватывает акцент
  артиста на его странице (var(--artist-accent)). `app/globals.css`.
- [x] **9.3** Hover-only интерактивные элементы теперь видны при фокусе с
  клавиатуры (`focus-visible:opacity-*`): стрелка трека (`track-list.tsx`),
  скачивание в покупках (`purchased-track-row.tsx`).
- [x] **9.4** Горячая клавиша `/` открывает поиск в навбаре (если не печатаешь
  в поле). `components/nav-search.tsx`.
- [x] **9.5** Отклик на «Поделиться» в фуллскрин-плеере — «Скопировано» при
  копировании ссылки (clipboard-фолбэк). `components/player/index.tsx`.

### Идеи на потом (интерактив/доступность)
- [x] Клавиши плеера: пробел = play/pause, ←/→ = перемотка ±5с (когда фокус не на
  интерактивном элементе и трек загружен). `components/player/use-player-hotkeys.ts`.
- [x] Видимый hint «/» в свёрнутом поиске — kbd-бейдж в кнопке (desktop, прячется
  при раскрытии). `components/nav-search.tsx`.
- [x] Тост-система (`components/toast.tsx`: zustand-стор + `toast()`/`toast.error()`,
  `<Toaster/>` в layout, стек до 3, автоскрытие 3.5с, клик закрывает). Подключены
  ошибки откатов: лайк (плеер + страница трека), подписка, отписка в профиле;
  плейлисты — оптимистичная галочка с откатом + тост «Добавлено в „X"».
- [x] `aria-current` на активном пункте навигации + подсветка раздела
  (`components/nav-link.tsx`, клиентский, `usePathname`).
- Hover-превью карточек (мини-инфо релиза/артиста при наведении) — частично
  закрыто 10.2/10.5 (quick-look релизов + hover-chip артистов).

## Этап 10 — Развитие «эталонного» приёма (раскрытие / shared-element / drag)
Эталон по фидбэку Danya: полноэкранный плеер и раскрывающийся поиск. Паттерн —
**компактное → богатое через shared-element + пружину + drag-dismiss + blur**.
Применяем тот же приём к другим поверхностям.
- [x] **10.1 Zoomable обложки.** Обложка релиза/трека разворачивается в
  полноэкранный просмотр (shared `layoutId`, blur-фон, свайп/клик/Esc), как плеер.
  `components/zoomable-cover.tsx`; применено на страницах релиза и трека.

Danya: «делай все» — реализуем все кандидаты.
- [x] **10.2 Quick-look релиза**: карточка релиза по клику разворачивается в
  оверлей с трек-листом и play (shared-element обложки, blur, drag/click/Esc),
  трек-лист лениво из `/api/v1/releases/[id]`. `components/release-quick-look.tsx`,
  применено на главной (свежие/скоро/активность) и в профиле артиста.
- [x] **10.3 Очередь в плеере**: фуллскрин-плеер раскрывает список очереди с
  drag-reorder (motion `Reorder`) и переходом по клику; dismiss-свайп отключается
  при открытой очереди, оверлей скроллится. `components/player/index.tsx`.
- [x] **10.4 Командная палитра ⌘K**: центр-оверлей (blur) с поиском (тот же
  `/api/v1/search`) + быстрая навигация, ↑↓/Enter/Esc, глобальный хоткей ⌘K/Ctrl+K.
  `components/command-palette.tsx`, смонтирована в `app/layout.tsx`.
- [x] **10.5 Hover-превью карточек**: при наведении на артиста на главной (desktop,
  с задержкой) всплывает мини-карточка с аватаром и инфо (peek). На тач — просто
  ссылка. `components/artist-hover-chip.tsx` (релизы уже имеют peek через quick-look).
- [x] **Артист-хедер с коллапсом при скролле**: когда hero уходит за верх,
  под навбар въезжает компактная полоска (аватар + имя + verified, блюр на
  фоне артиста; клик — плавный скролл наверх). Сентинел + IntersectionObserver
  на `#main-content`. `app/artists/[slug]/artist-collapse-bar.tsx`.
- [x] Тилт обложек 3D — `components/tilt.tsx` (пружинный наклон за курсором,
  только мышь, выключается при reduced motion): featured-обложка главной и
  ZoomableCover (внутренний слой, чтобы не мешать layoutId-морфу).
  (Клавиши плеера и тост-система — сделаны, см. «Идеи на потом».)

---

## Журнал выполнения
- **1.1** — `app/profile/unfollow-button.tsx`: компактная иконка вместо текста.
- **2.1** — `app/dashboard/profile/edit-profile-form.tsx`: 8 пресетов палитры.
- **3.1–3.2** — `components/nav-search.tsx`: раскрывающийся поиск с live-результатами.
- **4.1** — `app/template.tsx`: opacity-crossfade переходов (h-full, без поломки shell).
- **5.1** — `components/video-embed.tsx` + `lib/embed.ts`: видео-фасад под сайт.
- **8.1** — `app/profile/followed-artists.tsx`: оптимистичная отписка.
- **6.1–6.2** — менеджер треков (rename/delete/reorder) + роут tracks/[id], optimistic.
- **7.1–7.2** — главная-хаб (`app/page.tsx`) + `queries/discovery.ts`, навбар почищен.
- **5.2** — `components/video-player.tsx` + `lib/youtube-api.ts`: кастомный YouTube-плеер.
- **9.1–9.5** — фокус/`::selection`/клавиша «/»/фидбэк шеринга (полиш доступности).
- **10.1** — `components/zoomable-cover.tsx`: разворот обложек как у плеера.
- **10.2** — `components/release-quick-look.tsx`: peek релиза с трек-листом и play.
- **10.3** — очередь в фуллскрин-плеере с drag-reorder.
- **10.4** — `components/command-palette.tsx`: палитра ⌘K (поиск + навигация).
- **10.5** — `components/artist-hover-chip.tsx`: hover-превью карточек артистов.
