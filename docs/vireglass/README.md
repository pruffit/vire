# VireGlass

Материал «жидкое стекло» для интерфейсов Vire. Сейчас реализован **только на Android**
внутри мобильного приложения.

| Документ | О чём |
|---|---|
| `architecture.md` | карта текущей реализации: слои, параметры, ограничения |
| `ADR-001-rendering-architecture.md` | решение по архитектуре рендеринга и доказательства |
| `benchmarks/` | замеры: стенд, протокол, результаты |
| `../architecture/vireglass-spec.md` | спецификация материала и стратегия по платформам |

---

## Статус компонентов

| Компонент | Статус | Где |
|---|---|---|
| AGSL-линза (преломление, хроматическая и сферическая аберрация) | **Реализовано**, Android 13+ | `modules/glass-lens` |
| Skia-поверхность (фаска, Френель, блик, кромка, тень, иконка) | **Реализовано** | `components/liquid-glass.tsx` |
| Эластичная деформация при перетаскивании | **Реализовано** | там же |
| Плоское стекло панелей (блюр + тинт + рамка) | **Реализовано** | `components/glass.tsx` |
| Фолбэк ниже Android 13 (аффинная лупа) | **Реализовано** | `components/liquid-glass.tsx` |
| Стенд доступа к бэкдропу | **Экспериментально** | `screens/glass-lab.tsx` |
| Стенд замера масштабирования | **Экспериментально** | `screens/glass-bench.tsx` |
| Общий бэкдроп на N поверхностей | **Отложено** — замер снял основание (ADR-001 §5) | — |
| Поверхностная оптика для прямоугольных панелей | **Планируется** | — |
| Морфинг, гироскоп, шкала качества | **Планируется** | — |
| iOS-рендерер | **Не поддерживается** (нет публичного API доступа к фону) | — |
| Веб-рендерер | **Не поддерживается** | — |

---

## Известные ограничения

- **Настоящее стекло доступно только вне экранов** — таб-бару и мини-плееру. Внутри
  экрана оно деградирует до полупрозрачной плашки: положить стекло внутрь его же цели
  захвата нельзя, дерево RenderNode замыкается и рантайм падает. ADR-001 §2.
- **Стоимость растёт линейно, но коэффициент ничтожен:** ~0.2–0.3 мс GPU на поверхность
  на реальном устройстве. 10 стёкол — 0.00% пропущенных кадров на 120 Гц.
  `benchmarks/2026-08-28-scaling-device-release.md`. Эмуляторная оценка (4.3–5.2 мс)
  была завышена в ~20 раз.
- **Преломление требует Android 13+.** Ниже — равномерное увеличение, это лупа, не линза.
- **Измерено на одном устройстве** (Dimensity 7200 Ultra, Android 16, release, 120 Гц).
  Слабое железо, 60/90 Гц, крупные панели и длительная нагрузка не проверены.
- **Геометрия считается дважды** (SKSL и AGSL), константа `BEVEL` синхронизируется вручную.

---

## Стенд

Стенд заменяет собой приложение и включается переменной окружения — по умолчанию выключен,
продовый путь не задевает.

**Проверка корректности** (зонды) — годится эмулятор:

```bash
emulator -avd VireMusic_Test -gpu host   # без -gpu host смысла нет
cd apps/mobile
EXPO_PUBLIC_GLASS_LAB=1 npx expo start --dev-client
```

**Замер производительности** — только физическое устройство и только release.
Эмулятор завышает стоимость GPU примерно в 20 раз и для этого непригоден:

```bash
cd apps/mobile/android
EXPO_PUBLIC_GLASS_LAB=bench ./gradlew assembleRelease
adb install -r app/build/outputs/apk/release/app-release.apk
adb shell am start -n com.anonymous.viremobile/.MainActivity
```

Сцены: 1 / 2 / 3 / 6 / 10 поверхностей и контроль (0). Протокол и метрики —
`benchmarks/2026-08-28-scaling-device-release.md`.

Зонд `glass-lab` переключается по кнопке между тремя режимами: сырая выборка, выборка
с маркером живости шейдера, карта альфы. Последний однозначно показывает, пришло ли
в шейдер хоть что-нибудь.
