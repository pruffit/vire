# Роадмап до 1.0.0 — «оставшиеся 10%»

> Зафиксировано 10 июня 2026 по повестке Danya. Порядок блоков — приоритет.
> Статус: `[ ]` не начато · `[~]` в работе · `[x]` готово.
> Обновлено 12 июня 2026: v1.0.0 выпущен, CI/CD настроен (блок 7). Закрыты блоки
> 4 (алгоритмические плейлисты), 5 (пользовательские плейлисты на главной),
> 5а (оболочка: прелоадеры/футер/соглашения/куки). Блок 2 — почти весь готов.
> Жанры расширены до 69, настроения до 18 (v1.0.28). Почта — Brevo (не Resend).

---

## 1. Волна ступень 2 — адаптивный алгоритм потока

**Сейчас (ступень 1):** взвешенный SQL — совпадение mood-тегов + близость BPM +
тональность + шум (`packages/db/src/queries/wave.ts`). Без персонализации,
без поведенческих сигналов. **Это главный незакрытый продуктовый блок.**

**Куда идём:**
- [ ] Поведенческие сигналы из `play_events`: дослушивания vs скипы
  (`duration_played_sec / duration`), переслушивания (повторные сессии),
  источник запуска. Скип на волне = негативный сигнал к похожим трекам.
- [ ] Профиль вкуса слушателя: агрегат по лайкам, дослушиваниям, mood-тегам
  лайкнутых треков (материализуется по крону, не на лету).
- [ ] Анти-усталость: декей повторов (трек слышали недавно → штраф),
  баланс «знакомое/новое».
- [ ] Учёт любимых моментов (`favorite_moments`) как сильного позитивного сигнала.
- [ ] Деградация: при пустом профиле (аноним/новичок) — текущая ступень 1.

## 2. Метаданные треков и релизов — заполняет артист

> Решение Danya (11.06.2026): музыкант сам заполняет жанр, настроение и т.д.
> Уточнение (12.06.2026): BPM и тональность теперь определяются **автоматически**
> при транскоде (анализ аудио в воркере), теги файла — запасной вариант;
> в админке есть бэкфил «Заполнить BPM / Key».

- [x] Селект жанра в формах создания/редактирования релиза — `<select>` с
  `<optgroup>` по категориям (v1.0.28).
- [x] Отображение жанра на витрине: мета-строка на странице релиза.
- [x] Mood-теги в дашборде: `MoodPicker` подключён в `track-manager.tsx`
  (до 5 настроений на трек).
- [x] Права в `PUT /api/v1/tracks/[id]/moods` — проверяется владение треком
  (release.artistProfileId === artist.id), не только наличие профиля.
- [x] Жанры трека: `GenrePicker` (до 3) — группы по категориям + поиск (v1.0.28).
- [x] BPM/тональность — автоопределение при транскоде + бэкфил в админке.
- [ ] Ручная правка BPM/тональности артистом в менеджере треков (опционально —
  авто-анализ закрывает основную потребность).
- [ ] Жанр в волну: совпадение жанра — ещё один вес в score ступени 1.
- [ ] Фильтр/секция по жанрам на витрине (каталог артистов, отдельная подборка).
  Поиск уже показывает встречающиеся жанры чипами; выделенного фильтра нет.

## 3. Безопасность — аудит и доводка

**Сейчас есть:** Auth.js v5 (JWT, httpOnly), роли в `proxy.ts` (/dashboard,
/admin) + `requireAdmin` в server actions, zod-валидация входа на API-роутах,
проверка владения в сервисах (core), FLAC только по presigned URL, YooKassa
webhook верифицируется через API, секреты в env, S3-бакеты разделены
(vault приватный / stream публичный), CSRF — внутри Auth.js, CSP + security
headers (`next.config.ts`), rate limiting (Redis fixed-window), лимиты загрузки.

**Дыры, закрыть до 1.0:**
- [x] Rate limiting на API (follow, like, upload, play, presence).
- [x] Security headers: CSP, X-Frame-Options, Referrer-Policy, Permissions-Policy.
- [x] Лимиты загрузки: размер FLAC, mime/magic bytes валидация в `lib/upload`.
- [x] `pnpm audit --audit-level=high` в gates CI/CD.
- [~] Бэкапы Postgres: `scripts/backup.sh` есть — проверить, что крон активен на VPS.
- [ ] Dependabot/Renovate для авто-обновления зависимостей.
- [ ] Пентест-проход по чек-листу OWASP Top 10.

## 4. Алгоритмические плейлисты на главной ✓

- [x] Генерация подборок (по mood-тегам, поведению, свежести) — материализуются
  по крону/кнопке в `playlists` с системным владельцем (`editorial.ts`,
  `POST /api/v1/admin/editorial`, кнопка в админке «Подборки»).
- [x] Лайк плейлиста: `playlist_likes`, счётчик, optimistic-кнопка; лайкнутые в /profile.
- [x] Секция «Подборки» на главной (`EditorialPlaylistCard`).

## 5. Пользовательские плейлисты на главной ✓

- [x] Секция «Плейлисты слушателей» на главной — публичные (`getPublicUserPlaylists`).
- [x] Страница плейлиста доступна гостю.
- [x] Обложка-коллаж из обложек треков.

## 5а. Оболочка сайта: прелоадеры, футер, соглашения, куки ✓

- [x] **Прелоадеры**: `loading.tsx`-скелетоны для всех тяжёлых маршрутов
  (главная, /artists, артист, релиз, трек, профиль, лента, дашборд).
- [x] **Футер**: внутри скролл-области app-shell — логотип, год, версия,
  ссылки на /terms, /privacy, /feedback, «Стать артистом».
- [x] **Страницы соглашений**: `/terms` и `/privacy` (актуализированы под
  провайдеры Yandex/Google/Telegram). Перед массовым продом — показать юристу.
- [x] **Cookie-баннер**: компактное уведомление, выбор в localStorage.

## 6. Документация фич

- [ ] `docs/features/` — по файлу на фичу: что делает, где код (страницы/роуты/
  сервисы/таблицы), какие env нужны, известные ограничения. Покрыть ВСЁ:
  аутентификация (+привязка провайдеров), профиль (тема, аватар), релизы и треки
  (загрузка, транскодинг, BPM/key-анализ, статусы), плеер (HLS, очередь, волна,
  клавиши), лайки, подписки, плейлисты (+editorial), mood-теги, жанры, любимые
  моменты, шеринг, live-присутствие, посты, лента/главная, поиск (+⌘K),
  countdown/scheduled, видео-эмбеды, SEO/JSON-LD, дашборд (+аналитика),
  админка (+health), рассылки (Brevo).
- [ ] В CLAUDE.md — правило: новая фича не считается готовой без файла в features/.

## 7. Инженерная зрелость ✓ (частично)

- [x] Ветки: `dev` — разработка, `main` — стабильный, PR-флоу.
- [x] Версионирование: semver, тег `v1.0.0` выпущен (12.06.2026).
- [x] CI (GitHub Actions): typecheck + lint + test + audit:design + build на `dev` и PRs.
- [x] CD: один пайплайн по тегу `vX.Y.Z` — gates → сборка образов → деплой на VPS.
- [x] VPS: Timeweb Cloud, Docker Compose + Caddy, авто-TLS, swap.
- [ ] Стейджинг: второй VPS/окружение (когда оправдает нагрузка).
- [ ] Мониторинг: Sentry (ошибки), uptime-чек, алерты на failed-джобы очередей.

## 8. Этап 2 — прямые продажи (бэкенд готов, UI отвязан)

> Покупок в Этапе 1 нет: purchase-UI отвязан от витрины до старта Этапа 2.
> Бэкенд сохранён (purchase/download/webhook-роуты, `download-button.tsx`,
> `purchased-track-row.tsx` — неподключены).

- [ ] YooKassa боевая настройка: SHOP_ID/SECRET_KEY + вебхук в кабинете ЮKassa.
- [ ] Вернуть purchase-UI на публичные страницы.
- [ ] Тесты роутов purchase/webhook (не покрыты).

---

## Справка

**Обратный отсчёт до релиза.** Артист создаёт релиз с будущей датой и жмёт
«Запланировать» → SCHEDULED. Публичные запросы показывают SCHEDULED только когда
`release_date <= now()` — релиз открывается сам, без крона. До этого висит
`CountdownBadge`. При публикации уходит рассылка подписчикам
(queue notify-release → **Brevo HTTP API**, `apps/worker`).

**Авто-теги треков.** BPM и тональность — автоанализ аудио при транскоде
(+ бэкфил в админке); mood — ставит артист; жанр — ставит артист (69 жанров,
сгруппированы). Полноценный ML-автотег жанра/настроения — за рамками 1.0.
