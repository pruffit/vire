# План: QA-батч v1.13 (мобильный плеер и peek-шторки)

Спека: `docs/superpowers/specs/2026-07-05-qa-batch-1-13.md`. Один Sonnet-агент,
файлы соседние. После — самокритика отдельным агентом, гейты, Playwright-верификация.

## Правка 1 — `apps/web/components/quick-look-sheet.tsx`

Строка 69: в className внутреннего motion.div шита заменить `max-h-[88vh]` →
`max-h-full`. Больше ничего. (Покрывает и playlist-, и release-quick-look —
хром общий.)

## Правка 2 — `apps/web/components/player/fullscreen.tsx`

Перевести dismiss-жест на явные зоны, по образцу `quick-look-sheet.tsx`:

- `const dragControls = useDragControls()` (импорт из `motion/react`).
- На корневом motion.div: `drag="y"` (безусловно), `dragListener={false}`,
  `dragControls={dragControls}` — остальные drag-пропсы (constraints/elastic/
  onDragEnd) как были. Условие `showQueue || lyricsOpen` убрать.
- Состояние `lyricsOpen` и `onOpenChange` у `<Lyrics>` в фуллскрине больше не
  нужны — убрать стейт и проп (сам проп в `lyrics.tsx` оставить, он опциональный;
  НЕ трогать `lyrics.tsx`).
- Полоска-ручка: заменить декоративный div (строка ~85) на зону
  `onPointerDown={(e) => dragControls.start(e)}` с большим hit-area:
  контейнер `fixed top-0 left-0 right-0 h-10 z-10 flex items-start justify-center
  pt-3 touch-none cursor-grab active:cursor-grabbing`, внутри визуальная полоска
  `w-12 h-1 rounded-full bg-white/20`. Вся ширина верха — легко попасть пальцем;
  кнопка-шеврон (top-5 right-5, z-10) остаётся ПОВЕРХ зоны — ей поднять z до z-20,
  чтобы тап по ней не начинал drag (или разместить зону `left-14 right-14` —
  выбрать что чище, шеврон должен работать).
- Обложка — вторая drag-зона: на motion.div обложки (layoutId="player-cover",
  строка ~95) добавить `onPointerDown={(e) => dragControls.start(e)}` и
  `touch-none` в className (cursor-grab там уже есть).
- `stopPropagation` в `FullscreenExtras` (onPointerDown) теперь не нужен —
  убрать; в `lyrics.tsx` НЕ трогать (переиспользуется, вреда нет).
- Проверить: контейнер остаётся `overflow-y-auto`; после правки у него не должно
  быть `touch-action: pan-x` (dragListener={false} не ставит).

## Правка 3 — `apps/web/components/player/controls.tsx`

В `PlayPauseButton` удалить restored-кольцо: блок
`{restored && (<span … animate-breathe … border artist-accent />)}` (строки ~136–142).
Логику `restored ? resumeRestored() : togglePlay()` и селектор `restored` НЕ трогать.

## Тесты/гейты

- Юнитов на эти компоненты нет; не добавлять (UI-жесты — Playwright-верификация
  оркестратором по готовым скриптам в скретчпаде).
- Гейты: typecheck, lint, test, audit:design (UI тронут), build — прогнать после правок.

## Верификация (оркестратор, Playwright, тач-эмуляция)

1. viewport 400×560 + играющий трек: у шита `getBoundingClientRect().top >= 16`,
   ручка видима; тач-скролл списка работает; свайп по шапке закрывает.
2. Фуллскрин: computed `touch-action` контейнера ≠ pan-x; при переполнении
   контент скроллится тачем; свайп вниз за ручку закрывает; свайп за обложку
   закрывает; тап по шеврону закрывает.
3. Мини-бар в restored: кольца нет (нет `.animate-breathe` в кнопке play).
