# QA-батч v1.12 — план реализации

Спека: `docs/superpowers/specs/2026-07-05-qa-batch-1-12.md`.
Четыре независимых воркстрима, параллельные сабагенты (Sonnet), файлы не пересекаются.

## WS-A: UI-баги (пункты 1–3)

- `apps/web/components/scroll-row.tsx` — новый клиентский `ScrollRow` (стрелки,
  overflow-детект через ResizeObserver+scroll, aria-hidden кнопки, hover-девайсы).
- `apps/web/components/home/wave-chips.tsx` — обернуть ряд в ScrollRow.
- `apps/web/app/(listener)/page.tsx` — рейл «Новое у подписок» в ScrollRow.
- `apps/web/app/(listener)/releases/page.tsx` + новый клиентский
  `releases-grid.tsx` — слайс 24 + «Показать ещё».
- `apps/web/app/layout.tsx` — body `overflow-hidden` → `overflow-clip`;
  `#main-content` + `(listener)/layout.tsx`/`admin/layout.tsx` скролл-пейны —
  `overflow-x-clip`.
- Тест: `app/__tests__/layout-shell.test.ts` — инвариант clip.

## WS-B: анализ жанра по требованию (пункты 4–5)

- Producer: `apps/web/lib/queue.ts` — очередь `analyze-genre` + enqueue-хелпер.
- Consumer: `apps/worker/src/workers/analyze-genre.worker.ts` — S3 download →
  `classifyTrackGenre` → `saveGenreSuggestions` + `decideAutoApplyGenres`;
  регистрация в `apps/worker/src/index.ts`.
- DB: переиспользовать существующие функции сохранения suggestions
  (`packages/db/src/queries/track-audio.ts`); добавить недостающее.
- API: `apps/web/app/api/v1/dashboard/tracks/[id]/analyze-genre/route.ts`,
  `.../genre-suggestions/route.ts`; `apps/web/app/api/v1/admin/tracks/[id]/...`
  аналогично. Сегменты строго как у соседних роутов. Тесты прав/валидации
  рядом (`route.test.ts`) по образцу существующих.
- UI: общий хук `apps/web/lib/use-genre-analysis.ts`; `components/genre-picker.tsx`
  (кнопка+поллинг); `apps/web/app/admin/tracks/[id]/edit/track-edit-form.tsx`
  (блок «Предложено моделью», подгрузка suggestions в page.tsx).
- Worker-тест: политика/джоб по образцу transcode-тестов (S3/классификатор — фейки).
- Док: `docs/features/auto-genre.md`.

## WS-C: шрифты (пункт 6)

- `apps/web/lib/fonts.ts` — 8 новых sans/display + 2 mono, `preload: false`,
  variable-имена, SANS_VARS/MONO_VARS/fontVariables.
- `apps/web/app/dashboard/profile/edit-profile-form.tsx` — пикер: имя шрифта
  рендерится его семейством.

## WS-D: подборки (пункт 7)

- `packages/db/src/queries/editorial-policy.ts` — чистые функции:
  `pickPersonalMoods(tasteMoods, sharedMoods)`, `MIN_PERSONAL_TRACKS = 5`,
  фильтр размера. Юнит-тесты (vitest в packages/db; если конфига нет — добавить
  минимальный, тесты только на чистые функции, без БД).
- `packages/db/src/queries/editorial.ts` — использовать политику; личные mood
  исключают общие MOOD текущего прогона; консолидировать `moodTrackIds`/
  `personalMixTrackIds` в одну параметризованную функцию.
- Док: `docs/features/curated-playlists.md`.

## Гейты (после мержа всех WS)

typecheck / lint / check:routes / test / audit:design / build (web) +
typecheck / test (worker). Самокритика отдельным Sonnet-агентом по Vire-чеклисту.
Ship: версия 1.12.0 в двух package.json, lockfile, CLAUDE.md статус.
